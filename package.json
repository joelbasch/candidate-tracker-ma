{
  "name": "eye-to-eye-candidate-tracker",
  "version": "1.0.0",
  "description": "Eye to Eye Careers - Candidate Placement Tracker",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "clean": "node -e \"const fs=require('fs'); if(fs.existsSync('tracker-data.json')) fs.unlinkSync('tracker-data.json'); console.log('Data reset complete.');\"",
    "prestart": "node -e \"const fs=require('fs'); if(!fs.existsSync('uploads')) fs.mkdirSync('uploads');\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
